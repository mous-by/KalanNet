# URL de base de l'API KalanNet (doit être joignable depuis le téléphone/émulateur,
# pas seulement depuis cette machine : utilisez l'IP LAN de votre machine pour un
# appareil physique ou l'émulateur Android, "localhost" ne fonctionne que pour
# le mode web (npm run web) et le simulateur iOS.
EXPO_PUBLIC_API_URL=http://localhost:8000/api/v1
